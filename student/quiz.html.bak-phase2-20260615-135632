<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>كويز 9 — دوري المذاكراوية</title>
  <link rel="stylesheet" href="/assets/css/main.css" />
</head>
<body>
<div id="quizPage" class="app-shell">

  <nav class="side-nav" id="sideNav">
    <div class="nav-brand">
      <img src="/assets/img/league-logo.png" alt="logo" class="nav-logo-img" />
      <div class="brand-title">⚽ دوري المذاكراوية</div>
      <div class="brand-sub">منصة المذاكرة التنافسية</div>
    </div>
    <ul class="nav-links">
      <li><a href="/student/home.html"><span class="nav-icon">🏠</span> الرئيسية</a></li>
      <li><a href="/student/report.html"><span class="nav-icon">📋</span> تقرير الإنجاز</a></li>
      <li><a href="/student/operation.html"><span class="nav-icon">⚡</span> Operation Room</a></li>
      <li><a href="/student/quiz.html" class="nav-active"><span class="nav-icon">📝</span> كويز 9</a></li>
      <li><a href="/student/doctor-question.html"><span class="nav-icon">💬</span> سؤال الدكتور</a></li>
      <li><a href="/student/hall-of-fame.html"><span class="nav-icon">🏆</span> Hall of Fame</a></li>
      <li><a href="/student/leaderboards.html"><span class="nav-icon">📊</span> جداول الدوري</a></li>
      <li class="nav-logout"><a href="#" onclick="logoutStudent();return false;"><span class="nav-icon">🚪</span> خروج</a></li>
    </ul>
  </nav>

  <div class="main-content">
    <header class="page-header">
      <button class="nav-toggle" id="navToggle">☰</button>
      <div class="page-title">📝 كويز 9</div>
    </header>

    <main class="page-body">
      <div id="quizList">
        <div class="state-box loading-box"><span class="spinner"></span><p>جاري تحميل الكويزات…</p></div>
      </div>

      <div class="section-title mt-3">🎯 درجاتك</div>
      <div id="gradesList">
        <div class="state-box loading-box"><span class="spinner"></span><p>جاري تحميل الدرجات…</p></div>
      </div>
    </main>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="/assets/js/supabase.js"></script>
<script src="/assets/js/utils.js"></script>
<script>
document.addEventListener('DOMContentLoaded', async () => {
  const student = requireStudentAuth();
  if (!student) return;
  initNavToggle();
  const db = window.supabaseClient;

  // Load quizzes
  const quizList = qs('#quizList');
  try {
    let query = db.from('quizzes').select('*').eq('is_visible', true).order('quiz_date', { ascending: false });
    if (student.grade_level) query = query.eq('grade_level', student.grade_level);
    const { data: quizzes } = await query;
    if (!quizzes || !quizzes.length) {
      quizList.innerHTML = emptyHtml('مفيش كويزات متاحة دلوقتي');
    } else {
      quizList.innerHTML = quizzes.map(q => `
        <div class="quiz-card">
          <div class="quiz-title">${safeText(q.title)}</div>
          <div class="quiz-meta">${q.subject ? q.subject + ' · ' : ''}${formatDate(q.quiz_date)}</div>
          ${q.quiz_url ? `<a href="${q.quiz_url}" target="_blank" rel="noopener" class="btn btn-primary btn-sm">فتح الكويز 🔗</a>` : '<span class="text-muted">الرابط قريباً</span>'}
        </div>
      `).join('');
    }
  } catch (err) {
    quizList.innerHTML = emptyHtml('خطأ في تحميل الكويزات');
  }

  // Load grades
  const gradesList = qs('#gradesList');
  try {
    const { data: grades } = await db
      .from('quiz_grades')
      .select('quiz_id, student_name, score, max_score, created_at, quizzes(title)')
      .or(`student_id.eq.${student.id},student_phone.eq.${student.phone}`)
      .order('created_at', { ascending: false });
    if (!grades || !grades.length) {
      gradesList.innerHTML = emptyHtml('مفيش درجات مسجلة بعد');
    } else {
      gradesList.innerHTML = `<div class="lb-table">` + grades.map(g => {
        const pct = g.max_score ? Math.round((g.score / g.max_score) * 100) : null;
        return `
        <div class="lb-row">
          <span class="lb-name">${safeText(g.quizzes?.title || 'كويز')}</span>
          <span class="lb-value">
            <span class="quiz-grade-chip">${formatNumber(g.score)}${g.max_score ? ' / ' + g.max_score : ''}${pct ? ' (' + pct + '%)' : ''}</span>
          </span>
        </div>`;
      }).join('') + `</div>`;
    }
  } catch {
    gradesList.innerHTML = emptyHtml('خطأ في تحميل الدرجات');
  }
});
</script>
<a href="https://t.me/Mahmoud_M_Hassan101BOT" class="support-float" data-support-link aria-label="support">💬</a>
</body>
</html>
